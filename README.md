# penumbra-nids
Two-headed network intrusion detection: a supervised known-threat classifier plus a benign-only novelty head for attacks it was never trained on. Honest evaluation —   prevalence-corrected metrics, matched alert budgets, bootstrap CIs. Alerts the SOC; never auto-blocks.
